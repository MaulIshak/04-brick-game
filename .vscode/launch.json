{
    "configurations": [
    {
        "type": "lldb",
        "request": "launch",
        "name": "Launch",
        "program": "${workspaceFolder}/out/main.exe",
        "args": [],
        "cwd": "${workspaceFolder}"
    }
    ]
}